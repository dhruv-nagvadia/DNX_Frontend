export interface SkeletonProps {
  /** Any CSS length. Defaults to filling the parent. */
  width?: string | number;
  /** Any CSS length. Defaults to 16px. */
  height?: string | number;
  radius?: string | number;
  className?: string;
}
